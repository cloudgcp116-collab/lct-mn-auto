# give a python function with parameters
def greet_user(name, age, country): # these are parameters
    print(f"Hello, {name}!")
    print(f"You are {age} years old.")
    print(f"You are from {country}.")

# Calling the function with arguments
greet_user("Lavu'sCloudTech", 2, "India") # these are arguments